-----------------------------
-- Custom core configuration
-----------------------------

local config = {}
config.options = {
  local_session = {},
}
config.options.local_session.dir = ".vim.local"
config.options.local_session.file = "session.vim"

return config
